# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | arch | Correction detection mechanism | Dual: programmatic (retries, stuck, reverts from activity logs) + self-report (dispatch prompt instructions) | gsdup hooks aren't available in gsd2; programmatic catches hard signals, self-report catches soft signals | Yes — if Pi SDK adds hook support |
| D002 | M001 | arch | Correction/preference storage format | JSONL in `.gsd/patterns/` | Append-heavy writes suit JSONL; matches gsdup's proven schema; separate from YAML preferences which are read-heavy config | No |
| D003 | M001 | arch | Quality level storage | Extend existing `preferences.md` YAML frontmatter | Quality is a preference, not a separate config; avoids config proliferation | No |
| D004 | M001 | arch | Quality enforcement mechanism | Prompt injection via template variables | gsd2 controls dispatch prompts programmatically; more reliable than embedding instructions in agent files (gsdup approach) | No |
| D005 | M001 | arch | Quality gate metrics storage | Extend existing `metrics.ts` ledger | Gate events are unit-scoped metrics; extending the ledger is cleaner than a separate JSONL file | Yes — if volume is too high |
| D006 | M001 | arch | Tech debt file location | `.gsd/TECH-DEBT.md` (project-level, not milestone-scoped) | Debt spans milestones; matches gsdup's proven design | No |
| D007 | M001 | pattern | Correction taxonomy | 14-category taxonomy from gsdup (7 code + 7 process categories) | Proven in production across gsdup v6.0; well-tested coverage | Yes — if new categories emerge |
| D008 | M001 | pattern | Preference confidence formula | Bayesian-ish: count / (count + 2) | Starts at 0.6 at threshold (3 corrections), asymptotically approaches 1.0; proven in gsdup | No |
| D009 | M001 | pattern | Bounded learning guardrails | 6 non-negotiable constraints from gsdup (20% max change, 3 min corrections, 7-day cooldown, user confirmation, permission checks, 5+ co-activations) | Safety boundary for self-modifying system; proven in gsdup | No |
| D010 | M001 | scope | Browser dashboard | Deferred | gsd2 has TUI dashboard; browser dashboard is a different product surface requiring a Node.js server, SSE, component system | Yes — if user demand |
| D011 | M001 | scope | Concurrent milestones | Out of scope | gsd2's sequential model with `/gsd queue` + programmatic session management eliminates the workspace isolation problem | No |
| D012 | M001 | arch | Implementation approach | New TypeScript modules referencing gsdup designs, not porting CJS code | Different runtime (Pi SDK vs Claude Code hooks), different patterns (async/await vs sync CJS), different integration surfaces | No |
| D013 | S01 | pattern | Correction I/O error handling | All correction I/O returns structured `WriteResult` with reason field — never throws | Corrections are observational data; failure to capture must never block auto-mode dispatch. Pattern consistent with `activity-log.ts` silent-catch approach | No |
| D014 | S01 | pattern | Programmatic detection conservatism | Prefer false negatives over false positives — only flag clear patterns (3+ errors, 3+ rewrites of same file, explicit retry markers) | Over-detection would produce noisy correction data that degrades preference quality downstream; gsdup reference also emphasizes precision | Yes — thresholds may need tuning |
| D015 | S01 | pattern | Self-report instruction placement | Static block via `{{corrections}}` template variable in execute-task.md; S03 replaces with dynamic recall data | Keeps self-report and recall injection on the same template variable so S03's upgrade is a drop-in replacement, not a new variable | No |
| D016 | S01/T02 | pattern | Kill switch preference reading | Read `correction_capture` directly from preferences.md frontmatter using cwd-relative path instead of `loadEffectiveGSDPreferences()` | `loadEffectiveGSDPreferences()` caches `PROJECT_PREFERENCES_PATH` at module load time via `process.cwd()`, making it impossible to redirect per-call via a cwd parameter. Direct file read is testable and functionally equivalent. | Yes — if preferences module adds cwd parameter |
| D017 | S01/T04 | pattern | Pi session entry transformation for correction detector | `transformSessionEntries()` in auto.ts converts Pi session format (`{type:"message", message:{role, content}}`) to detector format (`{type, tool, input, result}`) | The detector's `SessionEntry` format is simpler and testable with fixture data. Pi session entries use nested message wrappers with tool calls inside `content` arrays. A bridge function in auto.ts keeps the detector module clean and independently testable. | No |
| D018 | S01/T04 | pattern | Kill switch in auto.ts uses `loadEffectiveGSDPreferences()` (not direct file read) | auto.ts correction guards use `loadEffectiveGSDPreferences()?.preferences?.correction_capture === false` | Unlike the I/O module (T02/D016) which needs cwd-relative paths for test isolation, auto.ts always runs in the real project context where `process.cwd()` is correct. Using the existing preferences API is simpler and benefits from preference merging. | No |
| D019 | S02 | naming | Patterns preference module named `pattern-preferences.ts` | Avoids collision with existing `preferences.ts` (config preferences system) | `preferences.ts` already exports `GSDPreferences`, `loadEffectiveGSDPreferences()`. The new module is a different concern (learned patterns from corrections). Distinct name prevents confusion and import ambiguity. | No |
| D020 | S02 | pattern | CATEGORY_SKILL_MAP uses gsd2's actual skills | Map code.style_mismatch → frontend-design, process.implementation_bug → debug-like-expert, process.regression → debug-like-expert; all others → null | gsdup's map referenced skills that don't exist in gsd2 (typescript-patterns, code-review, gsd-workflow, session-awareness). Mapping to null produces `type: 'new_skill_needed'` suggestions, which is correct behavior — S03 or future work can add skills. | Yes — when new skills are added |
| D021 | S02 | pattern | Observer trigger timing: after task completion | `analyzePatterns()` called in auto.ts post-completion block, same granularity as correction emission | Per-task matches gsdup's PostToolUse hook frequency. Per-slice would be less responsive. The I/O cost is bounded (reads corrections + preferences once per task completion). | Yes — if I/O overhead is measured as problematic |
| D022 | S02/T03 | arch | Skill existence check uses `homedir()` not `getAgentDir()` | `homedir()` + `/.gsd/agent/skills/<name>/SKILL.md` instead of dynamic import of `getAgentDir()` | Pi SDK package uses ESM-only exports incompatible with `require()` in the test context. `homedir()` resolves to the same `~/.gsd/agent/skills/` directory. | Yes — if Pi SDK adds CJS exports or gsd2 agent dir moves |
| D023 | S02/T03 | pattern | Cooldown guardrail matches by target_skill or category | When target_skill exists, cooldown matches by skill name. When null, matches by category | Prevents cooldown from blocking unrelated categories that happen to map to the same null skill | No |
| D024 | S02/T04 | pattern | Per-correction promotion (inside for-loop) | `checkAndPromote()` called per correction rather than once after the loop | Each correction gets its own promotion check with independent threshold evaluation. A batch of 5 corrections might push different categories over threshold independently. | No |
| D025 | S03 | arch | Recall block embeds self-report instructions | `buildRecallBlock()` appends self-report text after dynamic recall data in the same `{{corrections}}` variable | In gsdup, recall and self-report are separate hooks. In gsd2, both share the `{{corrections}}` template variable. Embedding self-report in the recall block ensures agents continue to log corrections even when recall data is present. | No |
| D026 | S03 | pattern | Token estimation formula | `Math.ceil(text.split(/\s+/).filter(Boolean).length / 0.75)` with 3000 max and 20 footer reserve | Proven formula from gsdup recall injection. Word-based estimation is simple, fast (synchronous), and accurate enough for the ~3K budget constraint. No need for tiktoken or similar. | No |
| D027 | S03 | arch | GSD_HOME env var for user-level preferences | `process.env.GSD_HOME \|\| join(homedir(), '.gsd')` for `~/.gsd/preferences.json` path | Enables test isolation without polluting real `~/.gsd/preferences.json`. Matches gsdup's `getGsdHome()` pattern. | No |
| D028 | S03 | pattern | Cross-project promotion triggered from checkAndPromote | `promoteToUserLevel()` called in `checkAndPromote()` after successful `writePreference()` | Promotion is a natural extension of preference creation — when a project-level preference is written, check if it should promote to user-level. Non-fatal try/catch ensures promotion failure never blocks preference promotion. | No |
| D029 | S04 | pattern | Quality instruction content uses concrete tool names | Instructions reference `rg`, `find`, `resolve_library`, `get_library_docs`, `git diff` | Abstract instructions ("scan the codebase") are less actionable than concrete tool references. The agent knows these tools and can execute them directly. | Yes — if tool names change |
| D030 | S04 | pattern | Single `{{quality}}` template variable for pre+post instructions | One variable with internally labeled pre-task and post-task sections instead of separate `{{quality_pre}}` and `{{quality_post}}` | Simpler template wiring (one variable vs two), follows the `{{corrections}}` pattern. Internal labels make it clear which instructions are pre-task vs post-task. | No |
| D031 | S04 | pattern | Gate events flushed at central post-completion point | Gate events stored module-level, flushed to UnitMetrics in auto.ts central completion block | Central point already handles all unit lifecycle. Covers all unit types (not just execute-task). Consistent with how metrics are already collected. | No |
| D032 | S04 | pattern | Dashboard quality section via exported helper functions | `aggregateGateOutcomes()` and `formatGateSummaryLine()` exported for testing | Dashboard overlay class is tightly coupled to TUI infrastructure (Theme, requestRender). Exported helpers enable comprehensive testing without mocking the full overlay. Logic tested, not rendering. | No |
| D033 | S05 | pattern | TECH-DEBT.md uses structured markdown, not JSONL | Markdown with `## TD-NNN: Title` sections | TECH-DEBT.md is browsed by humans and agents during planning (read-heavy); sequential IDs enable cross-referencing ("see TD-003"); markdown headings enable section-level reading with existing file tools; status changes visible in git diffs. JSONL is for append-heavy machine-processed data (corrections/preferences). | No |
| D034 | S05 | pattern | Tech debt auto-logging via existing `{{quality}}` variable | Extend `buildQualityInstructions()` post-task section | D030 established single `{{quality}}` variable pattern. Adding tech debt text to existing standard/strict instruction constants keeps the pipeline simple — no new template variables needed. | No |
| D035 | S05 | pattern | Passive monitoring runs only after complete-slice, not after every task | Check `currentUnit.type === 'complete-slice'` before running | Plan-vs-summary drift analysis is only meaningful after the summary exists. Running after every task would have no data to compare against. | No |
| D036 | S05 | pattern | Drift observations use existing correction categories | `code.scope_drift` for expansion/contraction, `process.planning_error` for shift | The 14-category taxonomy from S01 already includes these categories. No new categories needed — reuse means drift observations flow through the existing correction → preference → recall pipeline. | No |
| D037 | M002 | arch | Vectra over Qdrant/Neo4j for semantic recall | Vectra (in-process, file-backed) as primary; skip Neo4j entirely; defer Qdrant to future power-user tier | gsd-pi ships as npm package with zero external deps. Vectra is pure TypeScript, file-backed JSON — no Docker, no server. Neo4j's graph queries don't match gsd-pi's actual query patterns (similarity, not traversal). Qdrant reserved for future scale needs (1000+ vectors). | Yes — if correction volume exceeds Vectra's brute-force limits |
| D038 | M002 | arch | Embedding model strategy | OpenAI text-embedding-3-small as default, Ollama as local fallback, behind abstract interface | OpenAI is pragmatic ($0.02/M tokens), gsd-pi already has provider infra. Abstract `embed()` interface avoids lock-in. Ollama support enables zero-API-key local mode. | Yes — if better local models emerge |
| D039 | M002 | arch | Semantic recall as enhancement, not replacement | Vector similarity augments category-based recall; falls back to category matching when no embeddings configured | Zero-config users must not lose existing R007 functionality. Embedding is opt-in via configuring an embedding model. | No |
| D040 | M002 | pattern | Async fire-and-forget embedding at correction write time | Embedding triggered after writeCorrection() succeeds, non-blocking, failure captured but never thrown | Follows D013 non-throwing I/O pattern. Embedding API latency/failure must not block the dispatch loop or correction capture. | No |
| D041 | M002/S01 | pattern | Fixture vectors for S01 testing, no real API calls | Tests use orthogonal unit vectors and mock providers — no OpenAI/Ollama calls | Real API calls would be slow, costly, flaky, and require credentials. Fixture vectors prove ranking logic deterministically. Real provider integration verified manually or in S02. | No |
| D042 | M002/S01 | pattern | Minimal metadata in Vectra items | Store only correction_to, diagnosis_category, scope, timestamp, correction_from — not full CorrectionEntry | Vectra metadata is JSON stored in memory. Full entries would waste RAM and include fields irrelevant to recall display. | Yes — if recall needs more fields |
| D043 | M002/S02 | pattern | Embedding config via env vars | `GSD_EMBEDDING_PROVIDER`, `GSD_EMBEDDING_MODEL`, `GSD_EMBEDDING_API_KEY` env vars | Simpler than preferences.md frontmatter; consistent with existing API key patterns; easily testable via process.env override | Yes — could add preferences.md support later |
| D044 | M002/S02 | pattern | Serialize concurrent embeddings via promise chain | Module-level promise chain in auto.ts ensures sequential Vectra writes | Vectra LocalIndex may not handle concurrent writes safely; promise chain adds negligible latency for fire-and-forget operations | No |
| D045 | M002/S02 | pattern | Embed correction_to text for storage, task context for query | Store embeddings of `correction_to` (lesson learned); query embeddings of task context (unit type + plan summary) | `correction_to` captures the actionable lesson; task context at dispatch time captures what the agent is about to do — similarity between these surfaces relevant past lessons | No |
| D046 | M002/S03 | pattern | Embedding cost as named constant, not config | `EMBEDDING_COST_PER_TOKEN = 0.00000002` ($0.02/1M) as a module constant | Pricing changes rarely; a constant is simple and grep-able. Over-engineering a config system for one number adds complexity without value. Ollama cost is hardcoded to 0 (local). | Yes — if multiple OpenAI embedding models need different rates |
| D047 | M002/S03 | pattern | Full vector index clear on correction rotation | `rotateVectorIndex()` clears entire index rather than selective deletion by timestamp | Correction corpora are small (<100 items). Re-embedding after rotation is cheap and avoids complex timestamp-based filtering. Cleared vectors are re-populated naturally as new corrections are written. | Yes — if correction volume grows |
| D048 | M002/S03 | pattern | Embedding cost accumulator follows gate events flush pattern | Module-level accumulator reset on flush, same as `gateEvents` in quality-gating | Proven pattern from S04. Prevents double-counting across units. Consistent with existing metrics pipeline. | No |
| D049 | M002 | process | NEVER pivot milestone scope without updating context doc and getting user confirmation | M002-CONTEXT said "research and evaluate" but execution built a full implementation. Context doc was never updated to reflect the pivot. User was blindsided by shipped code they expected to be research findings. | **Non-negotiable.** If a milestone's scope changes from research to implementation (or vice versa), STOP, update M00N-CONTEXT.md, and confirm with the user before proceeding. Never silently pivot scope. | No |
| D050 | M002 | process | NEVER let milestone artifacts contradict each other | M002-CONTEXT read as research ("go/no-go decision", "proof-of-concept") while roadmap/slices executed full implementation. Summaries were internally consistent but disconnected from context. User could not parse what actually happened. | **Non-negotiable.** All milestone artifacts (CONTEXT, ROADMAP, SUMMARY) must tell a coherent story. If the plan changes, update upstream docs first. | No |
| D051 | M003 | arch | Single unified status bar key for all GSD modes | `"gsd-mode"` replaces `"gsd-auto"`; shared `setGSDStatus()` helper | Auto-mode's `"gsd-auto"` key would conflict with chat/quick using separate keys. Single key ensures only one mode displays at a time and simplifies transitions. Auto-mode migrated to use the shared key. | No |
| D052 | M003 | arch | Always-on recall via `before_agent_start` hook | Inject `buildRecallBlock()` output into system prompt on every agent turn in GSD projects | Recall currently only flows during auto-mode dispatch via template vars. The `before_agent_start` hook fires on every turn — adding recall here means chat, quick, and regular sessions all benefit. Gated by kill switch and `.gsd/` directory existence. | No |
| D053 | M003 | arch | Quick mode artifacts in `.gsd/quick/` not `.gsd/milestones/` | Quick tasks write summaries to `.gsd/quick/<timestamp>/` | Quick mode exists specifically to avoid milestone ceremony. Storing in milestones/ would create confusion and interfere with state derivation. | No |
| D054 | M003 | arch | Chat persistence in `.gsd/conversations/` | Timestamped markdown files with structured summary | Conversations are not milestones or tasks. Separate directory avoids interfering with milestone structure. Markdown over JSONL because conversations are read by humans during review. | No |
| D055 | M003 | pattern | Chat-to-quick handoff via task list file | Chat creates `<session-id>-tasks.md` in `.gsd/conversations/`; quick discovers most recent task list | Simple file-based handoff. No IPC, no shared state. `/gsd quick` without `--` description checks for recent task lists. | No |
| D056 | M003/S01 | pattern | Recall duplication prevention: `isAutoActive()` gate | `before_agent_start` skips recall when auto-mode is active; auto-mode continues using `{{corrections}}` template variable with vector-enhanced recall | Option (a) from S01-RESEARCH: simpler, preserves auto-mode behavior exactly, avoids needing to compute a "delta" between basic and vector recall | No |
| D057 | M003/S01 | pattern | Status bar `"paused"` maps to `setGSDStatus(ctx, 'idle')` | Auto-mode pause clears the status bar rather than showing "paused" | The unified key only supports idle/chat/quick/auto per D051. "Paused" is an auto-mode internal state, not a user-facing mode. Clearing the bar is the correct behavior — auto is not running. | Yes — could add 'paused' to the mode union if users want it |
| D058 | M003/S02 | arch | Quick mode is single-session sequential dispatch, not state machine | One prompt, one session — agent handles research/plan/execute/verify as sections of the prompt | Auto-mode's multi-unit state machine is ceremony. Quick mode should feel lightweight. `showDiscuss()` lifecycle pattern (pending state + agent_end cleanup) is the right model. | No |
| D059 | M003/S02 | pattern | Quick mode relies on `before_agent_start` for recall, not template var | No `{{corrections}}` in quick-task.md — recall already injected by S01's `before_agent_start` hook | Avoids duplicating recall. Quick mode only needs `{{quality}}` for template injection. `before_agent_start` fires for all non-auto sessions. | No |
| D060 | M003/S03 | pattern | Chat end uses pendingChatEnd flag, not mode check | `endChat()` sets flag + dispatches summarization; `agent_end` checks flag before running `checkChatEnd()` | Chat is multi-turn — agent_end fires after every response. Without the flag, cleanup would run after the first reply, not at session end. | No |
| D061 | M003/S03 | pattern | Bare `/gsd quick` discovers chat task lists via `findRecentTaskList()` | Scans `.gsd/conversations/*/tasks.md` sorted by directory name descending | Simple file-based discovery. No IPC needed. Consistent with D055. Falls back to usage notification when no task list found. | No |
